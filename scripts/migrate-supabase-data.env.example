# Prefer setting these in the shell with DOUBLE-QUOTED passwords — NOT postgresql://
# URLs (zsh mangles [ ] and %; never use $postgresql://).
#
# If db.<ref>.supabase.co does not resolve, set host + user from Connect → Session pooler:
#   OLD_SUPABASE_DB_HOST=aws-0-....pooler.supabase.com
#   OLD_SUPABASE_DB_USER=postgres.fwinuwatqdvuexyivbwu

OLD_SUPABASE_DB_REF=fwinuwatqdvuexyivbwu
NEW_SUPABASE_DB_REF=dowrojncfrjenwtzopsr

OLD_SUPABASE_DB_PASSWORD="use-double-quotes-if-password-has-special-chars"
NEW_SUPABASE_DB_PASSWORD="..."

# MIGRATE_MODE=data
# MIGRATE_MODE=full
